import React from "react";
import { SideBar, MainContainer } from "./index";

const Body = () => {
  return (
    <div className="flex">
      <SideBar />
      <MainContainer />
    </div>
  );
};

export default Body;
